engine:
  name: tofu

notifications:
  summary:
    enabled: true
    mode: pull_request

workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['false']
        on_error:
          - type: gate
            token: foobar
            all_of: ['user:terrateam-test-user02']
      - type: init
      - type: plan

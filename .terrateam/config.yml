workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['false']
      - type: init
      - type: plan

notifications:
  plan:
    visible_on: never
  summary:
    enabled: true
    mode: pull_request

engine:
  name: tofu

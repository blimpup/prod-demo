#! /usr/bin/env python3

import hashlib
import json
import os

files = []

for root, dirs, fs in os.walk('./'):
    files.extend([
        {
            'path': os.path.join(root, f),
            # Force a bad id
            'id': '1',
        }
        for f in fs])

print(json.dumps(files))
